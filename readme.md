Answer 1: var is an older way to declare variables. It can be redeclared and its value can be changed.
let can change its value but it can not declared again once it is declared before in the same scope.
const's value can not be changed once it is declared. The value will always be same.

Answer 2: Spread operator is used to expand elements on an array of an object.

Answer 3: map creates a new array by modifying each element of the original array.
filter creates a new array keeping only the elements that matches to a specific condition.
forEach loops through an array and executes a function for each element. It does not return a new array.

Answer 4: An arrow function is a shorter method to write functions.

Answer 5: Template literals are a way to write strings using backticks. It allows the strings to write in many lines.
